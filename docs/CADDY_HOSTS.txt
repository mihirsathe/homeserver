# Add to /etc/hosts on each admin device that wants short hostnames.
# Replace 100.x.x.x with the Unraid host's tailnet IP:
#   tailscale ip -4    (run on the Unraid host)
#
# All admin services are reachable at http://<name>.lan/ once these
# entries are in place. Generated by scripts/generate-configs.py — re-run
# after adding services or changing HOSTNAME_SUFFIX.

100.x.x.x  radarr.lan sonarr.lan lidarr.lan prowlarr.lan sab.lan seerr.lan bazarr.lan tautulli.lan profilarr.lan
